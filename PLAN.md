# SNGA macOS 26 客户端实施计划

## 摘要

- 从空仓库创建 `SNGA.xcodeproj`，使用 Swift 6.3、SwiftUI、SwiftData、WebKit 和 Security.framework，最低支持 macOS 26。
- 界面采用原生 `NavigationSplitView`：账号、收藏板块、全部板块、论坛消息；登录和复杂帖子正文使用受控 `WKWebView`。
- 首版交付可直接运行和测试的本地 Xcode 工程，不包含 Developer ID 签名、公证或商店发布。

## 核心实现

- 账号与存储：
  - 通过内嵌 NGA 官方网页完成登录及验证码交互，不读取或保存密码。
  - 登录成功后识别用户 UID、名称和头像，仅将 NGA 会话 Cookie 按账号加密存入钥匙串。
  - SwiftData 保存账号元数据、当前账号、收藏、同步状态、签到记录和回复草稿；账号删除时清理对应钥匙串与本地数据。
  - 每个账号使用独立 `NGAClient` actor 和显式 Cookie 请求头，禁止共享 Cookie 容器，确保切换及并发轮询不会串号。

- 网络与解析：
  - 定义 `NGAForumService` 接口，覆盖板块目录、主题分页、帖子分页、回复、私信、提醒、收藏同步和签到。
  - 读请求优先使用 NGA 页面提供的结构化输出，异常时回退到 HTML 解析；写请求先读取官方表单及校验字段，再提交一次，结果不明确时不自动重复写入。
  - 使用 `URLSessionConfiguration.ephemeral`、请求限速、超时和有限重试；支持 UTF-8/GB18030 响应，统一识别登录过期、权限不足、频率限制和站点结构变化。
  - 通过 SwiftPM 固定使用 [SwiftSoup 2.13.6](https://github.com/scinfu/SwiftSoup/releases/tag/2.13.6) 解析和清理 HTML。

- 浏览与回复：
  - 支持板块目录、收藏快捷入口、主题列表分页刷新、帖子分页/连续加载、楼层信息、图片、表格、折叠内容和外部链接。
  - 帖子 HTML 删除脚本、表单、iframe 和事件属性，仅允许安全样式与远程图片；站内链接转换为原生导航，外部链接交给默认浏览器。
  - 回复支持指定楼层、引用、常用 BBCode、表情代码、预览和按“账号 + 帖子”保存草稿；提交成功后刷新并定位新楼层。
  - 首版不支持发布新主题和本地图片上传。

- 收藏、签到与消息：
  - 收藏按账号隔离，优先与 NGA 站端双向同步；本地操作立即生效并进入待同步队列。冲突时本地待提交操作优先，其余采用站端状态，排序始终保留在本机；站端不支持时自动转为本地模式。
  - 应用启动、回到前台以及运行期间跨过北京时间日期边界时，为全部有效账号检查签到；每日成功或“已签到”后不再重复，失败可手动重试。
  - 每五分钟轮询全部账号的私信与回复、引用、@提醒；首次同步只建立未读基线，之后仅对新增事件通知。
  - 支持收件箱、已发送、私信详情及回复已有私信；不支持新建陌生人私信、删除或批量操作。
  - 系统通知只显示账号和消息类型，不展示发送者或正文；点击通知后切换到对应账号并打开目标内容。

## 内部接口与状态

- 核心标识类型使用 `AccountID`、`ForumID`、`TopicID`、`PostID`、`MessageID`，避免跨对象误用裸整数。
- `NGAForumService` 提供 `forums`、`topics`、`threadPage`、`submitReply`、`messages`、`replyMessage`、`favorites`、`updateFavorite`、`checkIn` 等异步方法。
- 会话状态统一为有效、需重新登录、受限和临时故障；收藏使用已同步、本地待写、仅本地和冲突状态。
- 网络实现、解析器、数据仓库、签到调度器和消息轮询器均通过协议注入，UI 不直接依赖 NGA 请求细节。

## 测试与验收

- 使用脱敏响应夹具测试结构化输出和 HTML 回退，包括板块、主题、楼层、私信、提醒、收藏、签到、登录失效、编码异常和页面结构缺失。
- 验证两个账号并发请求、快速切换、轮询和签到时 Cookie 完全隔离，日志及错误信息不包含 Cookie。
- 测试收藏三方合并、离线回退、重新同步、每日签到幂等性、北京时间日期边界，以及消息轮询不会重复通知。
- 通过伪造网络层验证回复流程严格执行“获取校验字段—提交一次—确认结果”，网络结果不明时保留草稿且不重复发送。
- UI 测试覆盖添加两个账号、切换账号、收藏板块、浏览分页帖子、引用回复、查看私信和点击通知。
- 最终执行 Debug 构建、单元测试和 UI 测试；再由用户在登录页手动登录测试账号，完成真实签到、收藏同步、帖子回复和私信回复验收。

## 假设与边界

- 应用名称默认为“SNGA”，首版界面使用简体中文。
- 应用退出后不签到、不轮询、不安装登录项或独立后台辅助进程。
- NGA 未提供稳定公开 API，因此所有站点协议集中在适配层；页面结构变化时显示可诊断错误，不影响已保存账号和草稿。
- 首版不包含论坛搜索、发新主题、图片上传、完整私信管理、离线全文归档、自动更新和安装包制作。
